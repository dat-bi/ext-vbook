# Proxy listen
HOST=0.0.0.0
PORT=18080

# Phone (Vbook App debug server)
TARGET_HOST=192.168.0.40
TARGET_PORT=8080

# Where to store sessions (*.jsonl)
DATA_DIR=

# Logging level:
# - ai: minimal logs for AI (status/log/result/data), no raw headers
# - full: include incoming headers + decoded data + upstream_raw
LOG_LEVEL=ai

# Limit size of stored raw responses to avoid huge .jsonl files
MAX_LOG_CHARS=200000

# AI CLI (optional)
# Using OpenAI-compatible API.
AI_API_KEY=
AI_BASE_URL=https://api.openai.com
AI_MODEL=gpt-4o-mini

# Proxy URL for CLI (optional). If empty, default is http://127.0.0.1:<PORT>
PROXY_URL=
